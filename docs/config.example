# ~/.config/pnet-telnet/config 示例
# 可在此定义默认行为 (Linux / future Windows via path translation)
# 终端二选一: kitty / wezterm / xterm
TERM_BIN=kitty
# 每次点击清空日志
LOG_TRUNCATE=1
# 复用窗口策略: 1=存在则重建(清屏) 0=聚焦
FRESH_ON_FOCUS=1
# 已存在窗口聚焦开关
FOCUS_EXISTING=1
# 自动重连(0/1)
AUTO_RECONNECT=0
# 断开后间隔
RECONNECT_DELAY=5
# 使用 expect 注入（默认关闭）
USE_EXPECT=0
